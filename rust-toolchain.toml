# Pinned so `cargo build` works regardless of the machine's rustup default.
# Requires >= 1.85 for edition 2024; CI also uses this pinned toolchain.
[toolchain]
channel = "1.98.0"
components = ["rustfmt", "clippy"]
