# Tests non-default zk/veil features and Lean proofs.

name: Test

on:
  push:
    branches: [main]
  pull_request:
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  test-zk:
    name: Test zk (${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: arm64
            runner: 16cpu-linux-arm64
            rustflags: -C target-cpu=native
          - arch: x86_64-avx512
            runner: 16cpu-linux-x64
            rustflags: -C target-cpu=znver4
    runs-on:
      [
        runs-on,
        "runner=${{ matrix.runner }}",
        spot=false,
        "run-id=${{ github.run_id }}",
      ]
    env:
      CARGO_NET_GIT_FETCH_WITH_CLI: "true"
      RUSTFLAGS: ${{ matrix.rustflags }}
    steps:
      - name: Checkout sources
        uses: actions/checkout@v6

      - name: Get stable toolchain date (cache key)
        id: rust-version
        shell: bash
        run: |
          STABLE_DATE=$(curl -sS https://static.rust-lang.org/dist/channel-rust-stable-date.txt)
          echo "date=$STABLE_DATE" >> $GITHUB_OUTPUT

      - name: rust-cache
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            ~/.rustup/
            target/
          key: flock-test-${{ runner.arch }}-${{ runner.os }}-${{ steps.rust-version.outputs.date }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            flock-test-${{ runner.arch }}-${{ runner.os }}-${{ steps.rust-version.outputs.date }}-

      - name: Install Rust (stable + rustfmt + clippy)
        shell: bash
        run: |
          if ! command -v rustup &> /dev/null; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
            echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
            export PATH="$HOME/.cargo/bin:$PATH"
          fi
          rustup toolchain install stable --component rustfmt --component clippy
          rustup default stable
          rustc --version && cargo clippy --version

      - name: cargo clippy (active veil feature)
        run: cargo clippy --release --workspace --all-targets --features veil -- -D warnings

      - name: cargo test (active veil feature)
        run: cargo test --release --workspace --features veil

      - name: Succinct VEIL benchmark regression
        if: matrix.arch == 'x86_64-avx512'
        env:
          VEIL_BENCH_BATCH: 256
          VEIL_BENCH_RUNS: 5
          VEIL_BENCH_CHECK: 1
        run: cargo bench -p flock-prover --features veil --bench veil_vs_flock

  lean:
    name: Lean proofs (build + axiom audit)
    runs-on:
      [
        runs-on,
        "runner=16cpu-linux-x64",
        spot=false,
        "run-id=${{ github.run_id }}",
      ]
    steps:
      - name: Checkout sources
        uses: actions/checkout@v6

      - name: lean-cache
        uses: actions/cache@5a3ec84eff668545956fd18022155c47e93e2684
        with:
          path: |
            ~/.elan/
            lean/.lake/
          key: flock-lean-${{ runner.os }}-${{ hashFiles('lean/lean-toolchain', 'lean/lake-manifest.json') }}
          restore-keys: |
            flock-lean-${{ runner.os }}-

      - name: Install elan
        shell: bash
        run: |
          if ! command -v elan &> /dev/null; then
            curl https://elan.lean-lang.org/elan-init.sh -sSf | sh -s -- -y --default-toolchain none
            echo "$HOME/.elan/bin" >> "$GITHUB_PATH"
          fi

      - name: lake build
        working-directory: lean
        run: |
          lake exe cache get
          lake build

      - name: Axiom audit (standard axioms only, no sorry)
        run: scripts/lean-axioms.sh
